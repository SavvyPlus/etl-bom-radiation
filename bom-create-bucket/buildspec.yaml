version: 0.2

phases:
  install:
    commands:
      - pip install --upgrade pip
      - pip install -r bom-create-bucket/requirements.txt -t bom-create-bucket

artifacts:
  base-directory: bom-create-bucket
  files:
    - '**/*'
  type: zip

cache:
  paths:
    - /root/.cache/pip
